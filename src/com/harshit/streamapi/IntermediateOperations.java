package com.harshit.streamapi;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class IntermediateOperations {

	public static void main(String[] args) {
		// filter<Predicate<T> predicate);
//		Stream<String> stream1 = Stream.of("Harshit","Neeta","Vinay","Pooja");
//		Stream<String> stream2 = stream1.filter((String name)->name.length() >= 6);
//		List<String> nameList = stream2.collect(Collectors.toList()); // mutable in nature ( modifiable)
//		List<String> nameList2 = stream2.toList(); // Immutable in nature or non modifiable 
//		System.out.println(nameList);
//		System.out.println(nameList2);
		
//		// map
//		Stream<String> stream1 = Stream.of("Harshit","Neeta","Vinay","Pooja");
//		Stream<String> mapStream = stream1.map((String name)->name.toLowerCase());
//		System.out.println(mapStream.collect(Collectors.toList()));
		
		
		
	}
	
	
}

